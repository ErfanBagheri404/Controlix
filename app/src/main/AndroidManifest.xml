<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.TRANSMIT_IR" />
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Not required: app installs on any phone, degrades gracefully without IR hardware -->
    <uses-feature android:name="android.hardware.consumerir" android:required="false" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/Theme.Controlix"
        android:allowBackup="true">

        <receiver
            android:name=".widget.ControlixWidgetProvider"
            android:exported="true"
            android:label="@string/widget_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/controlix_widget_info" />
        </receiver>

        <!-- Issue #53: Tasker / home automation entry point. Exported on
             purpose, so AutomationState (default off) is the trust gate. -->
        <receiver
            android:name=".feature.ExternalReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.erfanbagheri.controlix.TRANSMIT" />
            </intent-filter>
        </receiver>

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden"
            android:screenOrientation="unspecified">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Share-sheet attachments only: the JSON bundle is written to app
             cache and handed to ACTION_SEND as a content:// URI. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/controlix_file_paths" />
        </provider>
        <service
            android:name=".quicksettings.ControlixTileService"
            android:exported="true"
            android:icon="@drawable/ic_tile_power"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
            <meta-data
                android:name="android.service.quicksettings.ACTIVE_TILE"
                android:resource="@xml/tile_provider_info" />
        </service>
    </application>
</manifest>
